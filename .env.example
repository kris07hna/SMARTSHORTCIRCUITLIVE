# Polling interval in milliseconds (default: 2000ms)
NEXT_PUBLIC_POLL_MS=2000

# Number of historical data points to fetch (default: 180)
NEXT_PUBLIC_HISTORY_LIMIT=180

# Short-circuit detection threshold in mA (default: 10mA)
NEXT_PUBLIC_SHORT_THRESHOLD_MA=10
